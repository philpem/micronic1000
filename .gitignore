/doc/site-html
/site-mkdocs
/doc/node_modules
__pycache__
*~

# emulator scratch output
expect.json
analysis/ram_bank_*.bin
analysis/battery_ram.bin

# local scratch / backups
opencode.json.old
session-*.md
!doc/research/session-log.md

# built ROM image -- reproducible byte-for-byte from build.py + micronic/micron1.bin
analysis/rom_exerciser/*.bin

# Connector probe burn images are built locally. Their pinned fingerprints
# remain in the adjacent, tracked JSON manifests.
analysis/rom_exerciser/releases/**/*.bin

# Disk-backed local Arduino toolchain/build cache (not /tmp tmpfs).
# The whole .cache tree is local scratch (PR bodies, review notes, caches).
/.cache/

# Local credential/secret files -- never commit.
/TOKEN
